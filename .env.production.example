# Servicio API Railway (preferir una referencia a la variable del servicio PostgreSQL).
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/sentDB
NODE_ENV=production
PORT=3000

# Ambiente del proyecto EAS (publico e incluido en el binario movil).
EXPO_PUBLIC_API_URL=https://api.example.com
