# Cuenta demo local

Correo: `demo@smartant.local`

Contraseña: `SmartAntDemo2026!`

Crear o asegurar la cuenta:

```powershell
npm run seed:demo --workspace @smart-ant/api
```

Usa la base definida en `DATABASE_URL`. La cuenta queda con `CRC` y `America/Costa_Rica` por defecto.
