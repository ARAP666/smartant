{
  "name": "smart-ant",
  "private": true,
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "start": "npm run start --workspace @smart-ant/api",
    "type-check": "npm run prisma:generate --workspace @smart-ant/api && npm run type-check --workspaces --if-present",
    "lint": "biome check .",
    "test": "vitest run",
    "build": "npm run prisma:generate --workspace @smart-ant/api && npm run build --workspaces --if-present"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.0",
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  },
  "dependencies": {
    "react": "19.1.0",
    "react-dom": "19.1.0"
  }
}
